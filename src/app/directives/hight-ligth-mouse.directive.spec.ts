import { HightLigthMouseDirective } from './hight-ligth-mouse.directive';

describe('HightLigthMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HightLigthMouseDirective();
    expect(directive).toBeTruthy();
  });
});
