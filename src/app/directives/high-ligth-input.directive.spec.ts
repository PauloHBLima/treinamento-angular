import { HighLigthInputDirective } from './high-ligth-input.directive';

describe('HighLigthInputDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLigthInputDirective();
    expect(directive).toBeTruthy();
  });
});
